generator client {
  provider = "prisma-client-js" //manditory -js
}

datasource db {
  provider = "postgresql"
}

//User model
model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())
}
