// What I installed

1. npm init -y
2. npm install express cors dotenv
3. npm install prisma @prisma/client
4. npm install @prisma/adapter-pg pg

// Dev-depandancy

1. npm i --save-dev @types/node
2. npm i nodemon --save-dev
